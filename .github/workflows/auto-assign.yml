name: Auto Assign

on:
  workflow_dispatch: 
  pull_request:
    types: [opened]

jobs:
  assign:
    runs-on: ubuntu-latest
    steps:
      - uses: kentaro-m/auto-assign-action@v2.0.1
        with:
          reviewers: Natuworkguy
